services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: campo
      POSTGRES_PASSWORD: campo
      POSTGRES_DB: campo_db
    ports:
      - "5432:5432"
    volumes:
      - campo_db_data:/var/lib/postgresql/data

volumes:
  campo_db_data: